source 'http://rubygems.org'

# Specify your gem's dependencies in kube-rails.gemspec
gemspec
gem 'less-rails', path: ENV['LESS_RAILS_SOURCE'] if ENV['LESS_RAILS_SOURCE']

group :test do
  gem 'rake'
end
